---
const {
  title = "",
  text = "",
  image = "",
} = Astro.props;
---

<section
  class="w-full"
>
  <div class="bg-gray-400 py-10 px-6 flex flex-col items-center justify-center"  style={`background-image: url(${image});`}>
    <h3 class="text-4xl font-bold mb-4">{title}</h3>
    <p class="text-lg leading-relaxed mb-4">
      {text}
    </p>
    <button class="btn">Leer más</button>
  </div>
</section>