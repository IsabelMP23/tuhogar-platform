---
const {
  title = "",
  text = "",
  image = "",
} = Astro.props;
---

<section
  class="w-full mb-20"
>
  <div class="bg-gray-400 py-10 px-6 flex flex-col gap-6 items-center justify-center h-96 bg-cover bg-center relative -z-10"  style={`background-image: url(${image});`}>
     <div class="absolute inset-0 bg-gray-900/70 -z-10"></div>
    <h3 class="text-4xl font-bold text-white text-shadow">{title}</h3>
    <p class="text-lg text-center text-white max-w-2xl text-shadow-2xs">
      {text}
    </p>
    <button class="bg-white px-4 py-2 rounded-3xl shadow cursor-pointer hover:bg-gray-200 transition-colors duration-300 font-semibold">Leer más</button>
  </div>
</section>